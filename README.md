# rule-based-detection-
**
🚀 Installation & Setup**



1️⃣ Install Dependencies
pip install scapy


2️⃣ Run the Script
Windows (Run as Administrator):
python capture.py

Linux / Mac:
sudo python capture.py


🧪 Testing the System

Start the script:
python capture.py
Generate traffic:
ping google.com
Open a browser to generate HTTP/HTTPS traffic
Check generated logs in:
logs.json
